# API base URL for the AI Agent Control Tower backend (FastAPI).
# Copy this file to `.env` and adjust if your backend runs elsewhere.
VITE_API_BASE_URL=http://localhost:8000

# Application display name (optional override).
VITE_APP_NAME=AI Agent Control Tower
