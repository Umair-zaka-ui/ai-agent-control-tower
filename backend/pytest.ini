[pytest]
markers =
    live_provider: hits a real, locally running model provider endpoint (e.g. Ollama) instead of a replayed fixture. Excluded by default -- run explicitly with `-m live_provider`. See docs/runtime/providers.md.
addopts = -m "not live_provider"
