"""Enterprise organization authorization hierarchy (Phase 4.3.3).

Platform → Organization → Business Unit → Department → Team → Project → Resources,
plus resource ownership and delegated administration. Provides the organizational
context every authorization decision is evaluated within (§14, §24).
"""
