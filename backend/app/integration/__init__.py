"""Phase 2.1.1 SRS ACT-INT-FR-001..010 — the Enterprise Integration Framework.

Sibling to ``app/runtime/`` — this package builds the *framework* an
enterprise's external systems plug into as governed Tools; the runtime
itself never learns what any of them are (``ACT-INT-FR-006``, the
"runtime-never-knows" principle — see ``base.py``'s module docstring).
"""
