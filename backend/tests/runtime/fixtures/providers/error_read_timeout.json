{
  "_note": "Documentation only -- like error_connection_refused.json, a timeout has no HTTP response body (the connection never completes in time). The corresponding test raises httpx.ReadTimeout directly from its transport handler rather than replaying this file."
}
